import React from 'react';
import styles from './sideControls.module.sass';

// this component could hold additional controls for the app if needed
const SideControls: React.FC = () => (
  <nav className={styles.sideControls} />
);

export default SideControls;
